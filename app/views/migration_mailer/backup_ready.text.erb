<%= t('mailers.backup_ready.heading') %>
===============================

Hello,

<%= t('mailers.backup_ready.intro', token: @migration.token) %>

<%= t('mailers.backup_ready.download') %>
<%= @download_url %>

<%= t('mailers.backup_ready.size', size: (number_to_human_size(@backup_size) if @backup_size)) %>
<%= t('mailers.backup_ready.expires', time: (@expires_at.strftime('%B %d, %Y at %l:%M %p %Z') if @expires_at)) %>
<%= t('mailers.backup_ready.expiry_warning') %>

<%= t('mailers.backup_ready.whats_next') %>

---
<%= t('mailers.common.eurosky_migration') %>
Migration Token: <%= @migration.token %>
