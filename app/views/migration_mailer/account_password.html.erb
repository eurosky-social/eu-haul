<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 30px;
      border-radius: 8px 8px 0 0;
      text-align: center;
    }
    .content {
      background: #f8f9fa;
      padding: 30px;
      border-radius: 0 0 8px 8px;
    }
    .password-box {
      background: white;
      border: 3px solid #667eea;
      padding: 25px;
      margin: 25px 0;
      border-radius: 8px;
      text-align: center;
    }
    .password {
      font-family: 'Courier New', monospace;
      font-size: 20px;
      font-weight: bold;
      color: #667eea;
      padding: 15px;
      background: #f0f0f0;
      border-radius: 4px;
      word-break: break-all;
      user-select: all;
      -webkit-user-select: all;
      -moz-user-select: all;
      -ms-user-select: all;
    }
    .warning-box {
      background: #fff3cd;
      border-left: 4px solid #ffc107;
      padding: 15px;
      margin: 20px 0;
      border-radius: 4px;
    }
    .info-box {
      background: white;
      border-left: 4px solid #667eea;
      padding: 15px;
      margin: 20px 0;
      border-radius: 4px;
    }
    .details {
      background: white;
      padding: 15px;
      border-radius: 4px;
      margin: 20px 0;
    }
    .details dl {
      margin: 0;
    }
    .details dt {
      font-weight: bold;
      color: #666;
      margin-top: 10px;
    }
    .details dd {
      margin: 5px 0 10px 0;
      color: #333;
    }
    .footer {
      text-align: center;
      color: #666;
      font-size: 14px;
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #ddd;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1><%= t('mailers.account_password.heading') %></h1>
    <p>Migration <%= @migration.token %></p>
  </div>

  <div class="content">
    <p>Hello,</p>

    <p><%= t('mailers.account_password.intro') %></p>

    <div class="password-box">
      <h3 style="margin-top: 0; color: #667eea;"><%= t('mailers.account_password.heading') %></h3>
      <div class="password"><%= @password %></div>
      <p style="font-size: 14px; color: #666; margin-bottom: 0;">Click to select and copy</p>
    </div>

    <div class="warning-box">
      <h3 style="margin-top: 0;"><%= t('mailers.account_password.security_notice') %></h3>
      <p><strong><%= t('mailers.account_password.save_password') %></strong></p>
      <ul>
        <li><%= t('mailers.account_password.tip1') %></li>
        <li><%= t('mailers.account_password.tip2') %></li>
        <li><%= t('mailers.account_password.tip3') %></li>
      </ul>
    </div>

    <div class="details">
      <h3><%= t('mailers.account_password.account_details') %></h3>
      <dl>
        <dt><%= t('mailers.account_password.new_handle', handle: '').split(':').first %>:</dt>
        <dd>@<%= @migration.new_handle %></dd>

        <dt><%= t('mailers.account_password.new_pds', pds: '').split(':').first %>:</dt>
        <dd><%= @migration.new_pds_host %></dd>

        <dt><%= t('mailers.account_password.email', email: '').split(':').first %>:</dt>
        <dd><%= @migration.email %></dd>
      </dl>
    </div>

    <div class="info-box">
      <h3 style="margin-top: 0;"><%= t('mailers.account_password.whats_happening') %></h3>
      <ol style="margin: 10px 0;">
        <% if @migration.create_backup_bundle %>
        <li><%= t('mailers.account_password.backup_step1') %></li>
        <li><%= t('mailers.account_password.backup_step2') %></li>
        <li><%= t('mailers.account_password.backup_step3') %></li>
        <% else %>
        <li><%= t('mailers.account_password.no_backup_step1') %></li>
        <li><%= t('mailers.account_password.no_backup_step2') %></li>
        <% end %>
        <li><%= t('mailers.account_password.no_backup_step3') %></li>
      </ol>
      <p style="margin-bottom: 0;"><%= t('mailers.account_password.track_progress') %> <a href="<%= @migration_url %>">Migration Status Page</a></p>
    </div>

    <div class="footer">
      <p>Migration Token: <code><%= @migration.token %></code></p>
      <p><%= t('mailers.common.eurosky_migration') %></p>
    </div>
  </div>
</body>
</html>
