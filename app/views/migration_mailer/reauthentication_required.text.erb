<%= t('mailers.reauthentication_required.heading') %>
=========================

Hello,

<%= t('mailers.reauthentication_required.intro', token: @migration.token) %>

<%= t('mailers.reauthentication_required.reason') %>

Account Details:
- Old Handle: @<%= @migration.old_handle %>
- New Handle: @<%= @migration.new_handle %>
- <%= t('mailers.reauthentication_required.step', step: @failed_step.to_s.gsub('Job', '').humanize) %>

<%= t('mailers.reauthentication_required.what_to_do') %>

1. Visit your migration status page:
   <%= @migration_url %>

2. Enter your password for @<%= @migration.old_handle %> to re-authenticate

3. The migration will continue automatically where it left off

YOUR DATA IS SAFE:
All migration progress has been preserved. You just need to re-authenticate so we can continue transferring your account.

---
Migration Token: <%= @migration.token %>
<%= t('mailers.migration_completed.email_footer', email: @migration.email) %>
