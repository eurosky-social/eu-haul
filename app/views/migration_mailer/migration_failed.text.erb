<%= t('mailers.migration_failed.heading') %>
==========================================

Hello,

<%= t('mailers.migration_failed.intro', token: @migration.token) %>

<%= t('mailers.migration_failed.account_details') %>
- <%= t('mailers.migration_failed.did', did: @migration.did) %>
- <%= t('mailers.migration_failed.old_handle', handle: @migration.old_handle) %>
- <%= t('mailers.migration_failed.new_handle', handle: @migration.new_handle) %>

<%= t('mailers.migration_failed.failure_info') %>
- <%= t('mailers.migration_failed.failed_step', step: @failed_step.to_s.humanize) %>
- <%= t('mailers.migration_failed.error', error: @error_message) %>
- <%= t('mailers.migration_failed.previous_retries', count: @retry_count) %>

<%= t('mailers.migration_failed.what_to_do') %>

1. <%= t('mailers.migration_failed.do_step1') %>
   <%= @migration_url %>

2. <%= t('mailers.migration_failed.do_step2') %>

3. <%= t('mailers.migration_failed.do_step3', token: @migration.token) %>

<%= t('mailers.migration_failed.common_issues') %>
- <%= t('mailers.migration_failed.issue1') %>
- <%= t('mailers.migration_failed.issue2') %>
- <%= t('mailers.migration_failed.issue3') %>

<%= t('mailers.migration_failed.important') %>
- <%= t('mailers.migration_failed.note1') %>
- <%= t('mailers.migration_failed.note2') %>
- <%= t('mailers.migration_failed.note3', token: @migration.token) %>

---
<%= t('mailers.common.eurosky_migration') %>
